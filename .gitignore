# secrets / env
.env
.env.local
.env.*.local

# Claude Code config — kept fully local except the paper-trading safety hook
CLAUDE.md
.claude/*
!.claude/settings.json

# Personal notes — never commit even if this file ends up in the project folder by accident
PERSONAL-NOTES*.md

# node
node_modules/
.next/

# python
__pycache__/
*.pyc
.venv/
venv/
.pytest_cache/

# ML tracking (local file-store MLflow data — keep runs out of git history)
mlruns/
mlartifacts/

# OS
.DS_Store
